//agrupar cada archivo dentro de una funcion para cada prueba
describe('grupo de pruebas', () => {
    test('hola mundo en jest', () => {});
    test('hola mundo', () => {});

}) //este archivo revisa 2 pruebas y se agrupan con un describe

test('hola mundo en jest', () => {

});

//it hace lo mismo que test, manda a ejecutar prueba
it('hola mundo en jest', () => {

});
