//en lugar de 
const nombre = "monitor";
const precio = 333;
const disponible = true;

//con los objetos se pueden agrupar:en una sola variable
//Object literal
const producto = {
    nombre: "monitor",
    precio: 333,
    disponible : true
}

console.log(producto);
